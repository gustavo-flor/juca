server:
  port: 8080
  shutdown: graceful
  servlet:
    context-path: /api
spring:
  application:
    name: juca-monolith
  main:
    banner-mode: off
  datasource:
    url: ${DATASOURCE_URL}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
springdoc:
  swagger-ui:
    path: /
  use-fqn: true